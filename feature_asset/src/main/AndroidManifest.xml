<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.tp.feature_asset">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_feature_asset">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>
    <application>
        <activity android:name="com.tp.fixedassetaccounting.feature.asset.presentation.newasset.view.BarcodeActivity" />
    </application>
</manifest>

